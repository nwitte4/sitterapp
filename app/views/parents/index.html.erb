<div class="parent-index">

  <h3>Parent's Profile: </h3>
  <br>
  <h5> Email: <%= @parent.email %> </h5>
  <% @email = @parent.email.split('@')[0]%>

  <div class="remove-sitter-option">
    <%= link_to "Remove #{@email.capitalize}", "/parents/#{@parent.id}", method: :post %>
  </div>
</div>
